package com.sample.xxx.login

import com.sample.xxx.base.BaseInteractor
import javax.inject.Inject

class LoginInteractor @Inject constructor( /*Pref helper, api helper*/) :
        BaseInteractor(), LoginMVP.Interactor{

    //Holds user repository instance

    //Calls for various login types
}